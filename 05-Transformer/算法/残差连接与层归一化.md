---
tags:
  - 类型/算法
---

# 残差连接与层归一化

残差连接把子层输入直接加到输出上：$y=x+F(x)$，为信息和梯度提供短路径。层归一化则对单个样本在特征维度上标准化，以稳定各层激活的尺度。

Transformer 中常见两种顺序：Post-LN 使用 $\operatorname{LN}(x+F(x))$；Pre-LN 使用 $x+F(\operatorname{LN}(x))$。深层模型通常更容易用 Pre-LN 稳定训练。
