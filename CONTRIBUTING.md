# 贡献指南

欢迎为本仓库贡献内容!无论你是同学、学长学姐，还是不了解这门课具体大纲的老师，只要能补充人工智能相关的通识知识，都欢迎参与。

先感谢您对本项目的关注与贡献！

## 可以贡献什么

- **课程笔记补充 / 勘误**:针对 `01`~`07` 现有章节的内容纠错、补充例子、改进表述。
- **通识知识**:不确定该放进哪一章、或是多章都会用到的基础概念、公式、算法，放进 [00-公共基础](00-公共基础/) 即可，不必先搞清楚整个课程大纲。
- **新章节**:如果课程后续增加了新主题，可以按现有结构新开一章。

## 目录与写作约定

- 每章一个文件夹，命名为 `NN-章节名`(如 `03-神经网络基础`)，文件夹内有一个同名总览文件 `NN-章节名.md`，汇总该章小节。
- 小节文件命名为 `N.M-小节名.md`(如 `3.1-前馈神经网络.md`)。
- [00-公共基础](00-公共基础/) 内部按类型再分子目录，目前有 `基本概念/` 和 `算法/`，收录会被多章引用的概念(如损失函数、正则化、激活函数等)。新增通识条目时优先看能否归入已有子目录。
- 笔记正文采用 Obsidian 风格双向链接 `[[概念名]]`，首次出现被其他章节引用的术语，尽量在 [00-公共基础](00-公共基础/) 里建一个词条并用双链指向它，避免重复定义。
- 索引类文件(如各章总览、`00-公共基础.md`)保留 frontmatter(`tags`、`aliases`)，新增词条时补充对应的一行表格，格式参照同文件里已有的行。

## 提交方式

1. 内容不确定归属或有争议时，建议先开 Issue 讨论。
2. 确定内容后 Fork 仓库、新建分支提交，再发起 Pull Request。
3. PR 说明中简单描述改动的章节/词条即可，无需详细的课程背景。

## 许可协议

本仓库采用 [CC BY 4.0](LICENSE) 许可。提交 PR 即表示你同意贡献内容以相同协议发布，并允许在仓库内以你的 GitHub 用户名署名。
